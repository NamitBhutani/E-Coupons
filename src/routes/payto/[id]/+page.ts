// import type { PageServerLoad } from './$types';
// import { supabase } from '$lib/supabaseClient';
// export const load: PageServerLoad = async ({ params }) => {
// 	const { data } = await supabase.from('users').select('vendorbalance').eq('id', `${params.id}`);

// 	return {
// 		vendorBalance: data
// 	};
// };
